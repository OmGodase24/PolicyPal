# Render AI Service Environment Variables
# Copy these to Render → policypal-ai-service → Environment tab

PORT=8080

# MongoDB Atlas (from Step 2.5)
MONGODB_URI=mongodb+srv://policypal_user:YOUR_PASSWORD_HERE@policypal-cluster.xxxxx.mongodb.net/policypal?retryWrites=true&w=majority

# OpenAI API (get from https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxx

# Frontend URL (from Step 6.6 - for CORS)
ALLOWED_ORIGINS=https://policy-pal-xxxx.vercel.app

# Backend URL (from Step 4.6)
BACKEND_URL=https://backend-production-xxxx.up.railway.app

# AI Service Configuration
AI_SERVICE_PORT=8080
LANGCHAIN_MODEL=gpt-4o-mini
LANGCHAIN_TEMPERATURE=0.7
LANGCHAIN_MAX_TOKENS=4000

# PDF Processing
PDF_MAX_SIZE_MB=50
UPLOAD_DIR=/tmp/uploads

# OCR Configuration (only if using OCR features)
# TESSERACT_PATH=/usr/bin/tesseract
# POPPLER_PATH=/usr/bin

# LangChain/OpenAI Advanced Settings (optional)
# OPENAI_ORGANIZATION=org-xxxxx
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_API_KEY=xxxxx

